# Session Handoff: 2026-08-25 22:37

## Resume here

Build the mail-gateway filter for the engagement stats. Until it exists, every
Click / Views / PDF / PNG number in `/#/admin` is mostly NCI's Microsoft Defender
scanner, not students. It is cleanly identifiable: datacentre country (IE/NL),
an identical per-credential signature (`1 open / 3 click / 4 views / 1 pdf / 1 png`
across 27 people), and arrival within seconds of the send.

## State now

- **37 CEAI students hold a live Level 2 · Agent Operator credential, and all 37
  have been emailed.** 25 carry a recorded send; the 12 from the first batch
  predate the stamp and are confirmed by delivery evidence.
- Level 2 exists in the credential system for the first time: `LEVELS[2]`, eight
  competencies, and a two-route criteria narrative (Terminal track OR H9CEAI at
  60%+). Both routes are named in the signed VC.
- A real bug was fixed on the way: `/api/issue` silently dropped `level`, so every
  badge would have rendered Level 1 regardless of what was sent.
- The PDF footer was being clipped. Measured, not guessed: Level 1 already
  overflowed the content box by 4px and Level 2 by 35px. Now 54px and 23px of slack.
- Engagement analytics are live on Cloudflare D1: email opens, tracked click-through,
  page views, PDF/PNG downloads, LinkedIn/Share/Copy presses, and social unfurls.
  No raw IP is stored, ever.
- Learners now see their certificate **inside the dashboard**: a gold pill in the
  header plus a permanent 🎓 path, fed by a new `/api/my` that returns at most one
  credential, matched on the caller's own token. This is the fallback for a
  filtered email.
- Bulk issuance exists, with one-credential-per-person enforced server-side, and
  who-has-been-emailed stored on the credential rather than in a browser tab.
- The shared-secret bypass of the issuer gate (`TEST_ISSUE_SECRET`) was **removed**
  from the source. Google sign-in as the issuer is now the only way in.

## What's left

1. Filter gateway traffic out of the engagement stats (see Resume here).
2. Revoke then delete the two test credentials: `y8i52`, `k9q16`. Both are live,
   public, and carry TEST in the recipient name.
3. Chase the three students who cleared 60 but could not be matched to an account:
   Ross Doherty (79.5), Fuat Aygin (76), Fabio Poli (66). None were issued.
4. Add a Stop button to "Send to all not yet emailed" (the bulk panel has one; this
   newer control does not).
5. Delete `certs-system/scripts/qa-live.mjs`. It is inert since the bypass was
   removed and still contains a dead hardcoded test secret. **Blocked**: the
   secret-scan hook refuses every commit that removes that line. Needs
   `CLAUDUS_GUARD_OFF=1` in the session env, which only Victor can set.
6. Heather O'Malley received a duplicate badge email during the run. A one-line
   note would save her wondering.

## Files touched

- `certs-system/src/lib/credential.js` — LEVELS[2], ISSUABLE_LEVELS, two-route criteria
- `certs-system/src/worker.js` — level persistence, /api/my, /api/send, /api/track, /api/stats, duplicate guard, NFC
- `certs-system/src/lib/stats.js` — new; D1 event log, bot filter, emailEvidence()
- `certs-system/src/lib/store.js` — email index, compare-and-delete unindex
- `certs-system/src/lib/email.js` — level-aware subject and body, open beacon, tracked links
- `certs-system/src/lib/templates.js` — PDF vertical rhythm, date no longer wraps
- `certs-system/src/pages.js` — page-button tracking, CRED_ID constant
- `certs-system/src/dashboard.js` — bulk panel, send controls, Emailed column
- `certs-system/src/lib/firebase-auth.js` — requireUser added, shared-secret bypass removed
- `certs-system/migrations/0001_events.sql`, `certs-system/wrangler.toml` — D1
- `certs-system/.loop/` — BRIEF, CRITERIA, CRITERIA-BULK, LOG, check.sh, check-served.sh
- `certs-system/LEARNINGS.md` — distilled rules
- `index.html` — cert pill, permanent path, Credential engagement card, sortable columns
- `ceai-l2-award/` — PLAN.md, LEVEL2-DEFINITION.md, issue-list.csv, issuance-runbook html

## Pointers

- Plan and runbook: `aibadge/ceai-l2-award/PLAN.md`, `issuance-runbook-2026-08-25.html`
- Level 2 definition awaiting Victor's sign-off: `ceai-l2-award/LEVEL2-DEFINITION.md`
- Loop contract and verdicts: `certs-system/.loop/LOG.md`
- Gates that must pass before any deploy: `.loop/check.sh` (analytics, 31 checks) and
  `.loop/check-served.sh` (the SERVED issuer console, 15 checks)
- D1: `aibadge-certs-stats`, binding `STATS_DB`. KV email index: `email:<addr>` -> ucid.
- Two cold-verifier reviews ran this session. Both returned OVERALL FAIL first time
  and both were right; every evidenced defect was closed and re-proved.
