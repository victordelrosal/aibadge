WHAT:         The full Inkwell "Inkwell Rewritten" re-engagement package per context.md §6:
              diagnosis.md, design-spec.md, /site (deployable three.js landing page),
              marketing.md (email + 3 posts), cold-verify.md. Live GitHub Pages URL.
WHY:          Demonstrate the rich-harness Octopus pipeline end to end: a named cohort,
              a sized prize computed from the CSV, a branded WebGL experience, GDPR-clean
              copy, and a cold verifier that catches one lie. The harness is the work.
WHO:          Lapsed under-35 Inkwell readers (the page); Aoife Brennan MD + the marker
              (the package). Marks are for the catch and the judgement, not a pretty page.
CRITERIA:     See CRITERIA.md (frozen). Drawn from context.md §6 deliverables + §7 cold-verify.
PRD:          no (the harness context.md IS the spec; BRIEF + CRITERIA suffice).
FLEET:        Purple = me (orchestrator + analysis + build + gate).
              Yellow/Margot = me (CSV analysis, diagnosis.md) - crux, kept in-house for accuracy.
              Red-Orange/Søren = subagent (design-spec.md).
              Green/Cathal = subagent (marketing.md).
              Blue/Priya = me (build /site three.js page).
              Yellow-as-cold-verifier = fresh subagent (cold-verify.md), blind to build reasoning.
LOOP BUDGET:  2 rounds. Round 1 = build everything. Round 2 = fix whatever cold verify fails.
EXIT:         GitHub Pages URL loads, 3D renders, form captures, fallbacks work; all 6
              deliverables present; cold verify passes its 6 frozen criteria and catches one lie.
DOWNGRADES:   Email capture: a documented Formspree/mailto fallback counts as "captures" (no
              backend exists, per §5). GitHub Pages URL: served from the existing aibadge repo
              path; if propagation lags, the local file + push is the evidence.
NOTE:         Provenance mismatch logged: the typed mission paragraph describes FlexHaus gym,
              but the imported context.md + CSV are Inkwell Books. Instruction was "read
              context.md and deliver everything IT asks for" + "do not ask questions; decide".
              context.md is self-contained and data-matched, so I execute Inkwell. Surfaced here
              and in the handoff per COMPASS (flag contradictions, do not silently pick).
